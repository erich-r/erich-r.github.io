# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you:
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: Erich Robbi's website
email: erich.robbi@unitn.it
description: >- # this means to ignore newlines until "baseurl:"
  My personal website that contains my projects, education, work experiences and my CV.
baseurl: "" # the subpath of your site, e.g. /blog
url: "" # the base hostname & protocol for your site, e.g. http://example.com
github_username:  erich-r

# Build settings
plugins:
  - jekyll-feed
  - jekyll-sitemap

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
# exclude:
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
#   - Gemfile
#   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/

# Jekyll ignores any file/folder starting with "_" by default, but Sphinx
# (used to build SciProg27/) puts its CSS/JS/assets in folders like that.
# Force those back in so they get copied into the built site.
# NOTE: Jekyll's include matching is by basename, not full path, so these
# apply anywhere in the source tree, not just under SciProg27/.
include:
  - _static
  - _sources
  - _images
  - _downloads

collections_dir: "my_collections"

collections:
  educations:
    output: true
  projects:
    output: true
  work-experiences:
    output: true
  publications:
    output: true
  talks:
    output: true
  teaching:
    output: true

sass:
  sass_dir: _sass
  style: compressed

domain: erich-r.github.io       # if you want to force HTTPS, specify the domain without the http at the start, e.g. example.com
url: https://erich-r.github.io  # the base hostname and protocol for your site, e.g. http://example.com

